Copier
#include "monty.h"

/**
 * init_global - Initializes the global variable
 *
 * Return: The initialized global variable
 */
global_t init_global(void)
{
	global_t global = {NULL, NULL, NULL, 0};

	return (global);
}
